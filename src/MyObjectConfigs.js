const myObjectConfigs = {
  CreateFilter,

}



function CreateFilter(array, filteredProperty, comparison ){
    const filteredConfig = {
        array:array,
        filteredProperty:filteredProperty,
        comparison: comparison
    }
    return filteredConfig
}
export default myObjectConfigs